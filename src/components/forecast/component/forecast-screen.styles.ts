import {StyleSheet} from 'react-native';

export const ForecastScreenStyles = StyleSheet.create({
  searchButton: {
    backgroundColor: '#ddd',
    marginHorizontal: 20,
    marginTop: 10,
    padding: 10,
    borderRadius: 5,
  },
});
